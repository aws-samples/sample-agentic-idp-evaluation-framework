---
id: "embedding_generation"
name: "Embedding Generation"
description: "Generate multimodal vector embeddings for text, images, documents, video, and audio using Nova Multimodal Embeddings. Enables semantic search and RAG."
category: "advanced_ai"
categoryName: "Advanced AI"
icon: "database"
defaultFormat: "json"
tags: ["embedding", "vector", "semantic", "search", "rag", "multimodal", "similarity"]
exampleInput: "Document pages, images, or text passages"
exampleOutput: "Vector embeddings (256-3072 dimensions) for indexing in vector stores"
support:
  embeddings: "excellent"
---

# Embedding Generation

Generate multimodal vector embeddings for text, images, documents, video, and audio using Nova Multimodal Embeddings. Enables semantic search and RAG.

## When to use

Use this skill when the user needs to multimodal vector embeddings for text, images, documents, video, and audio using nova multimodal embeddings. enables semantic search and rag..

## Example

**Input**: Document pages, images, or text passages

**Output**: Vector embeddings (256-3072 dimensions) for indexing in vector stores

## Output format

Default format: `json`

Returns structured JSON with typed fields.

## Method support

- **embeddings**: excellent
